package gr.pkcoding.peoplescope.di

import org.koin.dsl.module

val presentationModule = module {
    // ViewModels
    // viewModel { UserListViewModel(get(), get()) }
    // viewModel { (userId: String) -> UserDetailViewModel(userId, get(), get()) }
}